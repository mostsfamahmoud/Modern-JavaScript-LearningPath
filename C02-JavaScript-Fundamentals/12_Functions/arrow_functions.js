let greet = (name) => {
  console.log(`Hello, ${name}`);
};

// let greet = (name) => console.log(`Hello, ${name}`);

greet("Mostafa");

/*
let square = (x) => {
  return x * x;
};

console.log(square(4));
*/

let square = (x) => x * x;

console.log(square(4));
