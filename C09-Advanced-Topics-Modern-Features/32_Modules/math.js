/***************************************************************************
 **************************** Basic Export *********************************
 **************************************************************************/

// Regular Exports: Exporting individual functions or variables.

// Exporting a function to add two numbers
export const add = (a, b) => a + b;

// Exporting a function to subtract two numbers
export const subtract = (a, b) => a - b;

// (we'll import here)
